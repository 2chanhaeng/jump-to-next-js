import emailProvider from "./email";
import githubProvider from "./github";
import googleProvider from "./google";

const providers = [emailProvider, githubProvider, googleProvider];
export default providers;
